"""Daily assistant scan package.

Reads the connected Google account's Gmail and Tasks (plus recent Little Gerry
chat conversations) once a day to surface follow-up reminders, recommend tasks
(created only on approval), and auto-import Gemini meeting summaries into the
Knowledge Base (kept or removed on review).
"""
